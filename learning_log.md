# 学习日志 (Learning Log) - Markdown Preview Service

## Day 1 - 2026-03-19 (环境突围与云端初始化)
### 1. 环境搭建与决策转变
- **起初尝试**：在本地使用 VMware 安装 Ubuntu 虚拟机，遇到网卡消失、无法修复及底层驱动冲突。
- **果断决策**：意识到虚拟机维护成本过高，果断卸载 VMware，转向使用 **腾讯云轻量应用服务器 (Ubuntu 22.04)**，显著提升了开发环境的稳定性。

### 2. 服务器连接与基础运维
- 学习使用 SSH 协议通过公网 IP (`119.45.153.130`) 远程管理服务器。
- 掌握了 Linux 基础目录操作 (`mkdir`, `cd`, `ls`) 以及编辑器 `nano` 的使用。

### 3. Git 与代码托管
- 配置了 Git 全局身份信息（用户名及邮箱）。
- 在服务器初始化 Git 仓库，并成功关联 GitHub 远程仓库。
- **安全实践**：遭遇了 GitHub Token 在命令行泄露的风险，并及时进行了 Token 撤销与重置。

---

## Day 2 - 2026-03-20 (后端起步：FastAPI 与公网服务)
### 1. Python 开发环境
- 安装并配置了 `python3-venv`，创建了项目独立的虚拟环境(“考虑到云服务器（1核2G）的内存和磁盘资源极其有限，且本项目仅为轻量级 Web 服务，
  经过技术权衡，我选择使用 Python 内置的轻量级 venv 替代了体积庞大的 Anaconda/Miniconda，以实现更优的资源利用率和更快的部署速度。”)
- 学习了为什么在服务器上要使用虚拟环境来隔离依赖。

### 2. FastAPI 接口开发
- 安装了 `fastapi` 和 `uvicorn`。
- 编写了第一个生产级后端接口 `main.py`，实现 JSON 数据返回。

### 3. 网络与安全配置
- **防火墙穿透**：通过腾讯云控制台手动配置防火墙规则，开放了 `8000` 端口的 TCP 访问。
- **跨设备访问**：成功在本地 Windows 浏览器通过 `http://119.45.153.130:8000/` 访问云端 API。
- **测试技巧**：初探了 FastAPI 自动生成的 `/docs` 交互式文档界面。
---

## Day 3 - 2026-03-20 (核心功能：解析引擎实现)
- [x] **库引入**：成功安装并配置了 `python-markdown` 解析库。
- [x] **架构升级**：
    - 引入了 `Pydantic` 模型进行输入参数校验，增强了代码的鲁棒性。
    - 实现了从 `GET`（简单查询）到 `POST`（复杂数据处理）的接口跨越。
- [x] **功能实现**：
    - 完成了 Markdown 文本到标准 HTML 标签的实时转换。
    - 通过 FastAPI 的 `/docs` 交互界面验证了接口的正确性（HTTP 200）。
- [x] **Bug 修复**：解决了代码粘贴过程中产生的语法错误（SyntaxError），熟悉了 Linux 环境下的代码排查技巧。
- [x] **自动化测试**：引入 pytest 框架，编写并编写了针对 API 接口的单元测试案例，全部通过。
---

## Day 4 - 2026-03-20 (前端页面搭建与扩展语法支持)
- [x] **页面布局**：使用 HTML 和纯 CSS (Flexbox) 搭建了经典的“左侧编辑、右侧预览”的分栏界面。
- [x] **扩展渲染集成**：在前端页面成功引入了 Highlight.js 库实现代码块高亮，并引入了 MathJax 支持复杂的 LaTeX 数学公式渲染。
- [x] **前后端联调（超前完成部分 Day 5 任务）**：编写了 JavaScript 监听事件，通过 `Fetch API` 打通了前后端的数据流。现在左侧文本框的内容可以做到毫秒级的实时云端解析与右侧渲染。
- [x] **静态资源托管**：更新了 FastAPI 路由，通过 `StaticFiles` 成功实现了后端直接伺服静态 HTML 页面。
